/* https://github.com/AlejanPelaez9004/Pelaez-cop3330-assignment2/blob/main/src/main/java/ex27/EmployeeInputs.java

ISSUE IS IT PRINTS NULLNULLNULLNULL and doesn't have a proper ending, needs to be worked on...

 */

package ex27;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class AppTest {

    /*
    @Test
    void nameTrue() {

        CodeFor27 validate = new CodeFor27();
        boolean Result = CodeFor27.CheckFN("Tom");
        boolean expected = true;

        assertEquals(expected, Result);
    }

    @Test
    void nameFalse() {

        CodeFor27 validate = new CodeFor27();
        boolean Result = CodeFor27.CheckFN("y");
        boolean expected = false;

        assertEquals(expected, Result);
    }

    @Test
    void employeeZipcodeTrue() {

        CodeFor27 validate = new CodeFor27();
        boolean Result = validate.CheckZip("32553");
        boolean expected = true;

        assertEquals(expected, Result);
    }

    @Test
    void employeeZipcodeFalse() {

        CodeFor27 validate = new CodeFor27();
        boolean Result = validate.CheckZip("text");
        boolean expected = false;

        assertEquals(expected, Result);
    }

    @Test
    void employeeIdTrue() {

        CodeFor27 validate = new CodeFor27();
        boolean Result = validate.CheckID("AB-1234");
        boolean expected = true;

        assertEquals(expected, Result);
    }

    @Test
    void employeeIdFalse() {

        CodeFor27 validate = new CodeFor27();
        boolean Result = validate.CheckID("UM-TEST");
        boolean expected = false;

        assertEquals(expected, Result);
    }


     */
}